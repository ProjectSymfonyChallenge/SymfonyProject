<form method="post" action="{{ path('manager_hike_delete', {'slug': hike.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hike.id) }}">
    <button class="btn-delete">{{ 'hike.btn.delete'|trans }}</button>
</form>
